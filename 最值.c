#include<stdio.h>
int main() {
	int a[n],i,max,min;
	printf("请输入n值:");
	printf("请输入整数:");
	for(i=0; i<n; i++) {


		scanf("%d",&a[i]);
		max=a[0];
		min=a[0];
		if(max<a[i])
			max=a[i];
		if(min>a[i])
			min=a[i];
	}
	




}

